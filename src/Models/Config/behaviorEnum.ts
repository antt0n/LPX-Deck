/**
 * Behavior of pads
 */
enum behavior {
  /**
   * On press
   */
  trigger,
  /**
   * On activate / desactivate
   */
  toggle,
  /**
   * Option between category same type button
   */
  option
};
export default behavior;